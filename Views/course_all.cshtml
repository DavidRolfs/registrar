<h1> Registrar </h1>
<hr />
<h2>Courses:</h2>
@if (Model.Count != 0)
{
  <ul>
    @foreach (var course in Model)
    {
      <li><a href="/course/@course.GetId()">@course.GetName()</a></li>
    }
  </ul>
}
