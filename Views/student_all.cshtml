<h1> Registrar </h1>
<hr />
<h2>Students:</h2>
@if (Model.Count != 0)
{
  <ul>
    @foreach (var student in Model)
    {
      <li><a href="/student/@student.GetId()">@student.GetName(), @student.GetDoe().ToString("dd/MM/yyyy")</a></li>
    }
  </ul>
}
